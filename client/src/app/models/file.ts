export class File {
    filename: string;
    uploadDate: string;

    constructor(filename:string, uploadDate: string) {
        this.filename = filename;
        this.uploadDate = uploadDate;
    }   
}
